<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#89000000</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="colorPrimary">#0084B4</item>
        <item name="colorPrimaryDark">#BFE0EC</item>
        <item name="colorAccent">#0084B4</item>
        <item name="android:buttonStyle">@style/Widget.Button.Custom</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget.Button.Custom" parent="android:Widget.Button">
        <item name="android:textColor">#ffffff</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@drawable/rounded_button_white</item>
    </style>

    <style name="Widget.Button.Following" parent="android:Widget.Button">
        <item name="android:padding">1dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:background">@drawable/following_button</item>
    </style>

    <style name="Widget.Button.NotFollowing" parent="android:Widget.Button">
        <item name="android:padding">1dp</item>
        <item name="android:textColor">#89000000</item>
        <item name="android:background">@drawable/not_following_button</item>
    </style>

</resources>
